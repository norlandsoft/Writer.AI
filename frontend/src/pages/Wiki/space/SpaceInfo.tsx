import React from "react";

interface SpaceInfoProps {
  spaceId: string;
}

const SpaceInfo: React.FC<SpaceInfoProps> = props => {
  return (
    <div>
      Info
    </div>
  );
}

export default SpaceInfo;